const {createApp} = Vue;

createApp ({
    data(){
        return{
            value: 'ciao',
        }
    }

    // Voglio agganciare l'input a value, così che sia l'utente a modificare il value














}).mount('.main-centrale');